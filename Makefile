all: nvim tmux zsh

tmux:
	@echo "Installing TMP for tmux config"
	git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

nvim:
	@echo "No commands for nvim yet"

zsh:
	@echo "No commands for zsh yet"
